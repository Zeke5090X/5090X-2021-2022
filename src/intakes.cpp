#include "main.h"
void intakes(){
   /* switch(intake_state){
			case '0' :
				intake.move_velocity(0);
				break;
			case '1' :
				intake.move_velocity(200);
				break;
			case '2' :
				intake.move_velocity(-200);
				break;
			default : 
				//intake_error = true;
				break;
		}
		*/
}